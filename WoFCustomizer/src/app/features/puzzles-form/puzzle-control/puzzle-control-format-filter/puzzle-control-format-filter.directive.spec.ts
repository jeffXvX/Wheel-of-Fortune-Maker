import { PuzzleControlFormatFilterDirective } from './puzzle-control-format-filter.directive';

describe('PuzzleControlFormatFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new PuzzleControlFormatFilterDirective(null);
    expect(directive).toBeTruthy();
  });
});
