import { PuzzlesViewportDirective } from './puzzles-viewport.directive';

describe('PuzzlesViewportDirective', () => {
  it('should create an instance', () => {
    const directive = new PuzzlesViewportDirective(null);
    expect(directive).toBeTruthy();
  });
});
