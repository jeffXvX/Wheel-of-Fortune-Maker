import { PuzzleInputFormatFilterDirective } from './puzzle-input-format-filter.directive';

describe('PuzzleInputFormatFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new PuzzleInputFormatFilterDirective();
    expect(directive).toBeTruthy();
  });
});
