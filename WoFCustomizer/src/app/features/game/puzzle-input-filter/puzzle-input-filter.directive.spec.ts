import { PuzzleInputFilterDirective } from './puzzle-input-filter.directive';

describe('PuzzleInputFilterDirective', () => {
  it('should create an instance', () => {
    const directive = new PuzzleInputFilterDirective(null);
    expect(directive).toBeTruthy();
  });
});
